{{- if .Values.mysql.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "smartinventory.fullname" . }}-mysql-init
  labels:
    {{- include "smartinventory.labels" . | nindent 4 }}
data:
  01-schema.sql: |
{{ .Files.Get "schema.sql" | indent 4 }}
{{- end -}}
